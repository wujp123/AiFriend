# 🔧 缓存问题解决方案

## 问题描述

点击 TRON 支付按钮时，仍然显示旧版本的"复制地址"功能，而不是新版本的"打开钱包支付"功能。

**原因**：浏览器缓存了旧版本的 JavaScript 文件。

---

## ✅ 解决方案（按顺序尝试）

### 方案 1：访问测试页面（最快）

访问专门的测试页面，自动检测版本：

```
https://wujp123.github.io/AiFriend/test-payment.html
```

这个页面会：
- 显示当前加载的版本
- 提供一键清除缓存功能
- 显示调试信息

### 方案 2：强制刷新（推荐）

**Windows / Linux**:
```
按 Ctrl + Shift + R
或
按 Ctrl + F5
```

**Mac**:
```
按 Cmd + Shift + R
或
按 Cmd + Option + R
```

### 方案 3：清除浏览器缓存

#### Chrome / Edge
1. 按 `F12` 打开开发者工具
2. 右键点击刷新按钮
3. 选择"清空缓存并硬性重新加载"

或者：
1. 设置 → 隐私和安全 → 清除浏览数据
2. 选择"缓存的图片和文件"
3. 时间范围选择"过去 1 小时"
4. 点击"清除数据"

#### Firefox
1. 按 `Ctrl + Shift + Delete`
2. 选择"缓存"
3. 时间范围选择"最近一小时"
4. 点击"立即清除"

#### Safari
1. Safari → 偏好设置 → 高级
2. 勾选"在菜单栏显示开发菜单"
3. 开发 → 清空缓存
4. 刷新页面

### 方案 4：使用隐身模式

打开隐身/无痕模式窗口：
- Chrome: `Ctrl + Shift + N` (Win) / `Cmd + Shift + N` (Mac)
- Firefox: `Ctrl + Shift + P` (Win) / `Cmd + Shift + P` (Mac)
- Safari: `Cmd + Shift + N`

然后访问：
```
https://wujp123.github.io/AiFriend/
```

### 方案 5：等待自动更新

已添加自动版本检测功能，下次访问时会：
1. 自动检测版本更新
2. 清除旧缓存
3. 提示用户刷新页面

---

## 🔍 如何确认新版本已加载

### 检查 1：查看控制台日志

1. 按 `F12` 打开开发者工具
2. 切换到 Console (控制台) 标签
3. 查找：
   ```
   🚀 AiFriend v2.0 - Wallet Integration
   ```

### 检查 2：测试支付按钮

1. 点击会员按钮
2. 选择任意套餐
3. 点击 TRON 支付
4. 查看弹窗内容：

**新版本** ✅：
```
[打开钱包支付] [复制地址] [取消]
```

**旧版本** ❌：
```
[复制地址] [已完成支付] [取消]
```

### 检查 3：查看页面源代码

1. 右键页面 → 查看页面源代码
2. 查找 `app.js` 引用
3. 应该显示：
   ```html
   <script type="module" src="app.js?v=2.0"></script>
   ```

---

## 📱 移动端 / Telegram 清除缓存

### Telegram Desktop
1. 设置 → 高级 → 管理本地存储
2. 清除所有
3. 重新打开 Mini App

### Telegram Mobile (iOS)
1. 设置 → 数据和存储 → 存储空间使用
2. 清除缓存
3. 重新打开 Mini App

### Telegram Mobile (Android)
1. 设置 → 数据和存储 → 存储空间使用
2. 清除缓存
3. 重新打开 Mini App

或者直接：
1. 长按应用图标
2. 应用信息 → 存储
3. 清除缓存

---

## 🎯 最终验证步骤

完成上述任何一个方案后，按以下步骤验证：

1. **访问主页**
   ```
   https://wujp123.github.io/AiFriend/
   ```

2. **打开控制台** (F12)
   - 查看是否显示 `v2.0` 版本号

3. **测试支付**
   - 点击会员按钮
   - 选择套餐
   - 点击 TRON 支付
   - 应该看到 **"打开钱包支付"** 按钮

4. **测试钱包连接**（需要安装 TronLink）
   - 点击"打开钱包支付"
   - TronLink 应该自动弹出
   - 看到预填充的金额和地址

---

## 🐛 仍然无法解决？

### 调试步骤

1. **访问测试页面**
   ```
   https://wujp123.github.io/AiFriend/test-payment.html
   ```

2. **查看显示的版本信息**

3. **点击"测试 TRON 支付"按钮**

4. **截图控制台输出**

5. **提供以下信息**：
   - 浏览器版本
   - 操作系统
   - 访问方式（浏览器/Telegram）
   - 测试页面显示的版本

### 临时解决方案

如果始终无法加载新版本，可以直接使用：

**方案 A：直接 URL 参数**
```
https://wujp123.github.io/AiFriend/?v=2.0&t=1234567890
```

**方案 B：使用测试域名**
```
https://wujp123.github.io/AiFriend/public/index.html
```

---

## 🎓 技术说明

### 为什么会有缓存问题？

1. **浏览器缓存策略**
   - 浏览器会缓存 `.js` 和 `.css` 文件
   - 提高加载速度，减少流量

2. **GitHub Pages 默认设置**
   - 默认缓存时间较长
   - 不会自动清除旧版本

### 我们的解决方案

1. **版本号查询参数**
   ```html
   <script src="app.js?v=2.0"></script>
   ```
   - 改变 URL，强制浏览器加载新文件

2. **自动版本检测**
   ```javascript
   const APP_VERSION = '2.0';
   if (savedVersion !== APP_VERSION) {
     // 清除缓存并刷新
   }
   ```

3. **用户提示**
   - 检测到新版本时自动提示
   - 引导用户刷新页面

---

## 📊 版本历史

| 版本 | 日期 | 主要功能 |
|------|------|---------|
| v1.0 | 2024-12 | 基础支付，复制地址 |
| v2.0 | 2024-12 | 钱包集成，一键支付 |

---

## ✅ 预防措施

为避免未来再次出现缓存问题：

1. **开发时**：使用隐身模式测试
2. **部署后**：清除浏览器缓存
3. **用户端**：提供清除缓存指引
4. **版本更新**：修改版本号参数

---

**更新时间**：2024年12月
**当前版本**：v2.0
